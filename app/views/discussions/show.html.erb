<p>
  <%= @discussion.body %>
</p>

<%= render partial: 'comments/template', locals: {commentable: @discussion, new_comment: @comment} %>


<%= link_to 'Edit', edit_discussion_path(@discussion) %> |
<%= link_to 'Back', discussions_path %>
